# Figure 12

Figure 12 workspace for `temporalDynamicHMotif`.

Current experiment:

- `figure12a/`: temporal strict-inc delta timing benchmark and plotting pipeline.
- `figure12b/`: temporal step-wise percentage breakdown (construction/deletion/insertion/update).
